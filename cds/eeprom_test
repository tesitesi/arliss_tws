#include <EEPROM.h>

int pressurePin = A1;

void setup() {
  pinMode(pressurePin, INPUT);
  for (int i = 0; i < 512; i++) {
    EEPROM.write(i, analogRead(pressurePin));
    delay(50);
  }
}

void loop() {}
